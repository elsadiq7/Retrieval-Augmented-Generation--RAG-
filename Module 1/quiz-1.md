### **Question 1**

What is the primary goal of using Retrieval Augmented Generation (RAG) system with a large language model (LLM)?

* To decrease the token count in prompts, making the system more cost-effective to operate.
* **To improve the quality and accuracy of the LLM’s responses by providing it with relevant external information.** ✅
* To completely replace the need for training large language models.
* To permanently retrain a model originally designed for another domain.

---

### **Question 2**

In a RAG system, what is the process called where the original user prompt is combined with retrieved information?

* Information indexing
* Knowledge base querying
* **Prompt augmentation** ✅
* Response generation

---

### **Question 3**

In what types of situations can RAG be particularly useful? *(Select all that apply)*

* When the LLM you're using has a particularly small context window
* When your LLM was trained extensively on the topics it will respond about
* **When the LLM will need to respond about your company's private and internal data** ✅
* **When the LLM will need to respond to questions about very recent events** ✅

---

### **Question 4**

When a RAG system receives your prompt, what happens with it next?

* The prompt goes directly to the LLM which then asks the retriever for help if needed.
* **The prompt goes to the retriever, which searches the knowledge base for documents relevant to the prompt which can help the LLM generate a response.** ✅
* The prompt is sent simultaneously to the retriever and the LLM, and whichever component produces the best response is used.
* The prompt is first sent to the LLM for refinement, which is then used to search the knowledge base.

---

### **Question 5**

In a RAG system, what role does the knowledge base play in supporting the LLM?

* It stores permanent changes to the LLM's internal parameters.
* It stores information about previously received prompts.
* **It provides access to specialized, private, or recent information that is absent from the LLM's original training data.** ✅
* It filters out inaccurate information after a response has been generated by the LLM.

---

### **Question 6**

Which best describes the role of the retriever?

* It retrieves the response from the LLM after it has been generated.
* **It searches the knowledge base for documents relevant to the user’s prompt.** ✅
* It retrieves the correct LLM from memory so that it can be used to respond to the prompt.
* It converts the prompt from text into tokens so that it can be processed by the LLM.

---

### **Question 7**

What is the purpose of the augmented prompt?

* To transform the retrieval process into a conversational format the LLM can understand.
* To permanently update the LLM's knowledge with retrieved information.
* **To combine the user's original prompt with the information retrieved from the knowledge base.** ✅
* To filter irrelevant information from the user's original question.

---

### **Question 8**

A user asks an LLM, "Why do carrots improve night vision?" The LLM responds incorrectly:

> "Carrots improve night vision because they are rich in vitamin A which enhances retinal function in low light conditions."

This myth was spread during WWII to conceal radar use. Why did the LLM provide this inaccurate response?

* The LLM randomly selected a few rare tokens early in the completion.
* **The LLM reproduced common text patterns from its training data even when those patterns contain inaccurate information.** ✅
* The LLM lacked any training data on the topic.
* The LLM knows the correct answer but intentionally misled the user.

---

### **Question 9**

An LLM is given the prompt “What a beautiful day, the sun is…” What does it do to generate the next token?

* It accesses pre-calculated completions for common phrases stored during training.
* **It processes the current text, calculates probabilities for thousands of possible tokens, then randomly chooses one based on those probabilities.** ✅
* It applies grammar rules to determine what type of word should come next.
* It looks for an exactly matching phrase in its training data.

---

### **Question 10**

Considering how LLMs segment text into smaller units, how many tokens would “California is uncommonly beautiful” likely be divided into?

* 1 token, as in "California is uncommonly beautiful"
* **4 tokens, as in "California", " is", " uncommonly", " beautiful"** ✅
* 6 tokens, as in "California", " is", " un", "comm", "only", " beautiful"
* 26 tokens, treating each individual character and space as a token
